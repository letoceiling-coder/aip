# План доработки бота согласно изначальному ТЗ

## Анализ различий между изначальным ТЗ и текущей реализацией

### Критические различия

1. **Приветственное сообщение** 
   - ❌ Текущее: Используется `bot->welcome_message` (короткий текст)
   - ✅ Требуется: Полный текст с упоминанием рейтингов, ассоциаций и достижений компании

2. **Кнопки категорий материалов**
   - ❌ Текущее: Используются `callback_data` (открывают категорию в боте)
   - ✅ Требуется: Должны быть URL-кнопки, ведущие на веб-сайт `https://www.ap-group.ru/author-materials/{slug}/`

3. **Описания категорий**
   - ❌ Текущее: Упрощенные описания в seeder
   - ✅ Требуется: Детальные описания согласно изначальному ТЗ

4. **Кнопка отзыва**
   - ❌ Текущее: "⭐ Оставить отзыв на Яндекс Картах" (с эмодзи)
   - ✅ Требуется: "Оставь отзыв на Яндекс Картах" (без эмодзи)

5. **Карточки материалов**
   - ❌ Текущее: Открывается категория с материалами для скачивания
   - ✅ Требуется: Прямые ссылки на веб-сайт для каждой категории

---

## План реализации

### Этап 1: База данных

#### 1.1. Добавить поле `external_url` в таблицу `bot_material_categories`

**Миграция:**
```php
Schema::table('bot_material_categories', function (Blueprint $table) {
    $table->string('external_url')->nullable()->after('description');
});
```

**Цель:** Хранение ссылок на веб-сайт для каждой категории.

**Примеры URL:**
- Структурирование: `https://www.ap-group.ru/author-materials/strukturirovanie/`
- Партнёрство: `https://www.ap-group.ru/author-materials/partnerstvo/`
- Проверки: `https://www.ap-group.ru/author-materials/proverki/`
- Наследование: `https://www.ap-group.ru/author-materials/nasledovanie/`
- Ликвидация: `https://www.ap-group.ru/author-materials/likvidatsiya/`
- Банкротство: `https://www.ap-group.ru/author-materials/bankrotstvo/`
- Работа с задолженностями: `https://www.ap-group.ru/author-materials/rabota-s-zadolzhennostyami/`
- Защита активов: `https://www.ap-group.ru/author-materials/zashchita-aktivov/`

#### 1.2. Обновить модель `BotMaterialCategory`

Добавить `external_url` в `$fillable`.

---

### Этап 2: Backend (Сервисы)

#### 2.1. Обновить `BotMenuService::getMaterialsListKeyboard()`

**Текущая логика:**
```php
$keyboard[] = [
    ['text' => $category->name, 'callback_data' => BotActions::MATERIAL_CATEGORY . $category->id],
];
```

**Новая логика:**
```php
if ($category->external_url) {
    // URL-кнопка (ведет на веб-сайт)
    $keyboard[] = [
        ['text' => $category->name, 'url' => $category->external_url],
    ];
} else {
    // Callback-кнопка (старая логика для обратной совместимости)
    $keyboard[] = [
        ['text' => $category->name, 'callback_data' => BotActions::MATERIAL_CATEGORY . $category->id],
    ];
}
```

**Цель:** Категории с `external_url` должны открывать веб-сайт, а не показывать категорию в боте.

#### 2.2. Обновить `BotHandlerService::showMainMenu()`

**Текущая логика:**
```php
$welcomeMessage = $bot->welcome_message ?? 'Добро пожаловать! Выберите раздел:';
```

**Новая логика:**
```php
$welcomeMessage = $bot->welcome_message ?? $this->getDefaultWelcomeMessage();
```

**Метод `getDefaultWelcomeMessage()`:**
```php
protected function getDefaultWelcomeMessage(): string
{
    return "Добро пожаловать в Аудиторско-консалтинговую группу «АИП» - одна из ведущих консалтинговых компаний России в области аудиторских, налоговых и юридических услуг!\n\n" .
           "Уже более 25 лет мы помогаем бизнесу успешно выходить из сложных ситуаций, выстраивать надёжную финансово-правовую систему и заранее предотвращать риски.\n\n" .
           "Компания входит в топ-30 в своей сфере по рейтингам ведущих аудиторско-консалтинговых агентств: «Эксперт РА», «ПРАВО-300», EuraAudit International, а также занимает 15-е место среди международных аудиторских организаций.\n\n" .
           "Являемся участником международной ассоциации EuraAudit International, что подтверждает соответствие нашей работы высоким международным стандартам качества и профессиональной этики.\n\n" .
           "Выберите, чем можем быть полезны 👇";
}
```

#### 2.3. Обновить `BotMenuService::getMainMenuKeyboard()`

**Текущая логика:**
```php
$reviewButton = $menu['review_button'] ?? '⭐ Оставить отзыв на Яндекс Картах';
```

**Новая логика:**
```php
$reviewButton = $menu['review_button'] ?? 'Оставь отзыв на Яндекс Картах';
```

#### 2.4. Обновить описание консультации в `BotHandlerService::showConsultationDescription()`

**Текущее:**
```php
$text = $consultation['description'] ?? 'Если вашему бизнесу нужна профессиональная юридическая поддержка, мы поможем.';
```

**Требуется:**
```php
$text = $consultation['description'] ?? 
    "Если вашему бизнесу нужна профессиональная юридическая поддержка, АИП возьмёт на себя все правовые вопросы.\n\n" .
    "Обращаясь к нам, вы избавляетесь на развитии бизнеса, а не на юридических рисках.";
```

---

### Этап 3: Seeder

#### 3.1. Обновить `BotMaterialCategorySeeder`

**Обновить описания категорий:**

1. **Структурирование:**
   ```php
   'description' => 'для построения эффективной юридической и финансовой структуры',
   'external_url' => 'https://www.ap-group.ru/author-materials/strukturirovanie/',
   ```

2. **Партнёрство:**
   ```php
   'description' => 'по юридическим связкам между учредителями и предотвращению корпоративных конфликтов.',
   'external_url' => 'https://www.ap-group.ru/author-materials/partnerstvo/',
   ```

3. **Проверки:**
   ```php
   'description' => 'в помощь для эффективной подготовки и прохождения',
   'external_url' => 'https://www.ap-group.ru/author-materials/proverki/',
   ```

4. **Наследование:**
   ```php
   'description' => 'по наследованию бизнеса, чтобы активы и управление не были утрачены.',
   'external_url' => 'https://www.ap-group.ru/author-materials/nasledovanie/',
   ```

5. **Ликвидация:**
   ```php
   'description' => 'компаний без последствий и претензий.',
   'external_url' => 'https://www.ap-group.ru/author-materials/likvidatsiya/',
   ```

6. **Банкротство:**
   ```php
   'description' => 'работа в процедуре и защита от субсидиарной ответственности',
   'external_url' => 'https://www.ap-group.ru/author-materials/bankrotstvo/',
   ```

7. **Работа с задолженностями:**
   ```php
   'description' => 'Чек-лист по управлению долгами и снижению рисков личной ответственности.',
   'external_url' => 'https://www.ap-group.ru/author-materials/rabota-s-zadolzhennostyami/',
   ```

8. **Защита активов:**
   ```php
   'description' => 'по защите активов в предпринимательских спорах.',
   'external_url' => 'https://www.ap-group.ru/author-materials/zashchita-aktivov/',
   ```

---

### Этап 4: Frontend (Админ-панель)

#### 4.1. Обновить форму категории материалов

Добавить поле для ввода `external_url` в компоненте `MaterialCategoryTree.vue` (форма создания/редактирования категории).

**Поле:**
- Название: "Ссылка на веб-сайт"
- Тип: `url`
- Описание: "Если указана, категория будет открывать эту ссылку вместо показа в боте"
- Валидация: URL формат

#### 4.2. Обновить API валидацию

Добавить валидацию `external_url` в `StoreMaterialCategoryRequest`:
```php
'external_url' => ['nullable', 'url', 'max:500'],
```

---

### Этап 5: Обратная совместимость

#### 5.1. Обработка категорий без external_url

Если `external_url` не указан, использовать старую логику (callback_data) для показа категории в боте.

#### 5.2. Обновление существующих категорий

Для существующих категорий можно:
- Либо оставить без `external_url` (будут работать по старой логике)
- Либо запустить обновление через seeder или отдельную команду

---

## Порядок выполнения

1. ✅ Создать миграцию для `external_url`
2. ✅ Обновить модель `BotMaterialCategory`
3. ✅ Обновить `BotMenuService::getMaterialsListKeyboard()`
4. ✅ Обновить приветственное сообщение в `BotHandlerService`
5. ✅ Обновить текст кнопки отзыва
6. ✅ Обновить описания консультации
7. ✅ Обновить seeder с новыми описаниями и URL
8. ✅ Обновить админ-панель (форма категории)
9. ✅ Обновить валидацию в Request
10. ✅ Протестировать работу

---

## Важные замечания

1. **Приоритет URL над callback:** Если указан `external_url`, категория всегда должна открывать ссылку, а не показывать категорию в боте.

2. **Обратная совместимость:** Категории без `external_url` должны продолжать работать по старой логике (открывать категорию в боте).

3. **Приветственное сообщение:** Можно оставить возможность настройки через `bot->welcome_message`, но по умолчанию использовать полный текст.

4. **Описания категорий:** Обновить в seeder, но оставить возможность редактирования через админ-панель.

---

## Тестирование

После реализации проверить:

1. ✅ Приветственное сообщение отображается полностью
2. ✅ Кнопки категорий с `external_url` открывают веб-сайт
3. ✅ Кнопки категорий без `external_url` открывают категорию в боте
4. ✅ Кнопка отзыва без эмодзи ⭐
5. ✅ Описание консультации соответствует ТЗ
6. ✅ Описания категорий в seeder соответствуют ТЗ
7. ✅ Админ-панель позволяет редактировать `external_url`

---

**Дата создания плана:** 2025-12-27  
**Статус:** Готов к реализации




