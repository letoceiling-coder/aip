# 🚀 Быстрый старт: Автоматический деплой

## Самый простой способ (GitHub Actions)

### 1. Настройте Secrets в GitHub

1. Перейдите: https://github.com/letoceiling-coder/aip/settings/secrets/actions
2. Нажмите "New repository secret"
3. Добавьте:
   - **Имя**: `DEPLOY_SERVER_URL`
   - **Значение**: `https://ваш-сервер.com` (без `/api/deploy`)
4. Добавьте еще один:
   - **Имя**: `DEPLOY_TOKEN`
   - **Значение**: токен из `.env` на сервере (значение `DEPLOY_TOKEN`)

### 2. Готово! 🎉

Теперь при каждом `git push origin main` автоматически:
1. GitHub Actions запускает workflow
2. Отправляется запрос на сервер
3. Сервер обновляет код из git
4. Выполняются миграции, обновляются зависимости

---

## Для локальной автоматизации

### Настройте pre-push hook:

**Windows:**
```bash
# Используйте .git/hooks/pre-push.bat
# Настройте переменные в .env или git config
```

**Linux/Mac:**
```bash
chmod +x .git/hooks/pre-push
# Настройте переменные в .env
```

**Настройка переменных:**
```bash
# Через .env
DEPLOY_SERVER_URL=https://ваш-сервер.com
DEPLOY_TOKEN=ваш-токен

# Или через git config
git config deploy.server-url "https://ваш-сервер.com"
git config deploy.token "ваш-токен"
```

---

## Проверка работы

```bash
# 1. Внесите изменение
echo "// Test" >> app/Providers/AppServiceProvider.php

# 2. Закоммитьте и отправьте
git add .
git commit -m "Test auto deploy"
git push origin main

# 3. Проверьте:
# - GitHub Actions (вкладка Actions в репозитории)
# - Логи на сервере: tail -f storage/logs/laravel.log
```

---

## Полная документация

См. [AUTO_DEPLOY_SETUP.md](AUTO_DEPLOY_SETUP.md) для всех вариантов настройки.




