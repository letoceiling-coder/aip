# 🚀 Настройка автоматического деплоя

Существует несколько способов автоматизировать обновление кода на сервере. Выберите наиболее подходящий для вашей ситуации.

## 📋 Варианты автоматизации

### 1. ✅ **GitHub Actions** (Рекомендуется)
Автоматический деплой при push в репозиторий GitHub.

### 2. **GitHub Webhook**
Webhook на сервере, который получает уведомления от GitHub при push.

### 3. **Git Pre-Push Hook** (Локально)
Автоматический деплой сразу после успешного `git push`.

### 4. **Git Post-Receive Hook** (На сервере)
Автоматический деплой при push в bare репозиторий на сервере.

---

## 🔧 Вариант 1: GitHub Actions (Рекомендуется)

### Преимущества:
- ✅ Не требует настройки на сервере
- ✅ Работает для любых разработчиков
- ✅ Можно настроить деплой для разных веток
- ✅ История деплоев в GitHub

### Настройка:

#### Шаг 1: Добавьте Secrets в GitHub

1. Перейдите в репозиторий: https://github.com/letoceiling-coder/aip/settings/secrets/actions
2. Добавьте следующие secrets:
   - `DEPLOY_SERVER_URL` - URL вашего сервера (например: `https://your-server.com`)
   - `DEPLOY_TOKEN` - токен из `.env` на сервере (значение `DEPLOY_TOKEN`)

#### Шаг 2: Файл уже создан
Файл `.github/workflows/deploy.yml` уже создан и настроен.

#### Шаг 3: Тестирование

```bash
# Сделайте любой коммит и отправьте в репозиторий
git add .
git commit -m "Test auto deploy"
git push origin main
```

GitHub Actions автоматически запустит деплой при push в ветку `main`.

---

## 🔧 Вариант 2: GitHub Webhook

### Преимущества:
- ✅ Мгновенное обновление
- ✅ Полный контроль на сервере
- ✅ Можно добавить проверку подписи

### Настройка:

#### Шаг 1: Роут уже добавлен ✅
Роут `/api/webhook/github` уже добавлен в `routes/api.php`. 
Контроллер проверяет либо подпись GitHub (если настроен `GITHUB_WEBHOOK_SECRET`), 
либо использует `DEPLOY_TOKEN` для авторизации.

#### Шаг 2: Настройте авторизацию

**Вариант A - Проверка подписи GitHub (рекомендуется):**

Добавьте в `.env` на сервере:
```env
GITHUB_WEBHOOK_SECRET=your_webhook_secret_here
```

**Вариант B - Использование DEPLOY_TOKEN:**

Если `GITHUB_WEBHOOK_SECRET` не настроен, webhook будет использовать `DEPLOY_TOKEN`.
В этом случае в настройках GitHub webhook добавьте токен в заголовок Authorization или используйте GitHub Actions (см. Вариант 1).

#### Шаг 3: Настройте Webhook на GitHub

1. Перейдите в репозиторий: https://github.com/letoceiling-coder/aip/settings/hooks
2. Нажмите "Add webhook"
3. Заполните:
   - **Payload URL**: `https://your-server.com/api/webhook/github`
   - **Content type**: `application/json`
   - **Secret**: (тот же, что в `GITHUB_WEBHOOK_SECRET`)
   - **Events**: выберите "Just the push event"
   - **Active**: ✅

#### Шаг 4: Проверьте работу

```bash
git push origin main
# Webhook автоматически запустит деплой
```

---

## 🔧 Вариант 3: Git Pre-Push Hook (Локально)

### Преимущества:
- ✅ Работает сразу после `git push`
- ✅ Не требует настройки GitHub
- ✅ Можно отключить для конкретных push

### Настройка:

#### Шаг 1: Установите hook

```bash
# Сделайте файл исполняемым
chmod +x .git/hooks/pre-push
```

#### Шаг 2: Настройте переменные

Вариант A - через `.env`:
```env
DEPLOY_SERVER_URL=https://your-server.com
DEPLOY_TOKEN=your_deploy_token
```

Вариант B - через git config:
```bash
git config deploy.server-url "https://your-server.com"
git config deploy.token "your_deploy_token"
```

#### Шаг 3: Тестирование

```bash
git push origin main
# После успешного push автоматически запустится деплой
```

#### Отключение для конкретного push:

```bash
SKIP_DEPLOY=1 git push origin main
```

---

## 🔧 Вариант 4: Git Post-Receive Hook (На сервере)

### Преимущества:
- ✅ Работает при push в bare репозиторий на сервере
- ✅ Не зависит от GitHub
- ✅ Мгновенное обновление

### Настройка:

**Этот вариант требует настройки bare репозитория на сервере.**

1. Создайте bare репозиторий на сервере:
```bash
mkdir -p /var/repo/your-project.git
cd /var/repo/your-project.git
git init --bare
```

2. Добавьте post-receive hook (пример в `.git/hooks/post-receive`)

3. Настройте рабочий репозиторий:
```bash
cd /path/to/your/project
git remote add deploy user@server:/var/repo/your-project.git
```

4. Теперь push в `deploy` автоматически запустит деплой:
```bash
git push deploy main
```

---

## 🎯 Рекомендуемая комбинация

**Для максимальной автоматизации используйте:**

1. **GitHub Actions** - для автоматического деплоя при push в GitHub
2. **Git Pre-Push Hook** - для мгновенного деплоя при локальном push (опционально)

Это обеспечит:
- ✅ Автоматический деплой при push в GitHub (для всех разработчиков)
- ✅ Мгновенный деплой при локальном push (для вас)
- ✅ История деплоев в GitHub Actions

---

## 🔐 Безопасность

### GitHub Actions:
- Используйте GitHub Secrets для хранения токенов
- Токены не будут видны в логах

### Webhook:
- Обязательно настройте `GITHUB_WEBHOOK_SECRET`
- Проверяйте подпись в контроллере

### Git Hooks:
- Не коммитьте токены в репозиторий
- Используйте git config или переменные окружения

---

## 📝 Настройка переменных

### На локальной машине (`.env`):
```env
DEPLOY_SERVER_URL=https://your-server.com
DEPLOY_TOKEN=your_secret_token_here
```

### На сервере (`.env`):
```env
DEPLOY_TOKEN=your_secret_token_here
PHP_PATH=/usr/bin/php
COMPOSER_PATH=/usr/local/bin/composer
```

### В GitHub Secrets:
- `DEPLOY_SERVER_URL` = URL вашего сервера
- `DEPLOY_TOKEN` = тот же токен, что на сервере

---

## 🧪 Тестирование

После настройки проверьте работу:

```bash
# 1. Внесите небольшое изменение
echo "// Test" >> app/Providers/AppServiceProvider.php

# 2. Закоммитьте и отправьте
git add .
git commit -m "Test auto deploy"
git push origin main

# 3. Проверьте:
# - GitHub Actions (если настроен) - вкладка Actions
# - Логи на сервере: tail -f storage/logs/laravel.log
# - API endpoint: curl https://your-server.com/api/deploy (с токеном)
```

---

## ⚠️ Важные замечания

1. **Не используйте автоматический деплой в production без тестирования**
2. **Всегда проверяйте логи после деплоя**
3. **Настройте уведомления об ошибках деплоя**
4. **Используйте разные токены для разных сред (dev/prod)**

---

## 🆘 Устранение проблем

### Деплой не запускается:
- Проверьте, что токен правильный
- Проверьте доступность сервера: `curl https://your-server.com/api/deploy`
- Проверьте логи: `tail -f storage/logs/laravel.log`

### GitHub Actions не запускается:
- Проверьте, что файл `.github/workflows/deploy.yml` существует
- Проверьте, что Secrets настроены правильно
- Проверьте вкладку Actions в GitHub

### Webhook не работает:
- Проверьте, что URL доступен из интернета
- Проверьте подпись в логах
- Проверьте, что webhook активен в GitHub

---

## 📚 Дополнительная документация

- [DEPLOY.md](DEPLOY.md) - Документация по команде деплоя
- [DEPLOY_DOCUMENTATION.md](DEPLOY_DOCUMENTATION.md) - Подробная документация
- [routes/api.php](routes/api.php) - API endpoints

